// state start
default
{
    // event start
    touch_start(integer num_detected)
    {
        llSay(0, "Just a test, dumbass."); // Channel 0 is for nearby chat. This line makes up the content of the event.
    }
    // event end
}
// state end